import { makeStyles } from '@material-ui/core'

export const styles = makeStyles({
    boxbtn: {
        display: 'flex',
        justifyContent: 'center',
        position: 'absolute',

    },

    boxmain:{
        paddingBottom: 0
    }
}) 