import { makeStyles } from '@material-ui/core'

export const styles = makeStyles({
    boxbtn: {
        display: 'flex',
        justifyContent: 'center',
        position: 'absolute',
        marginTop: 13

    },

    containerbtn: {
        display: 'flex',
        justifyContent: 'center'
    },

    boxmain:{
        
    },

    boxoptions: {
        marginTop: 20
    }
}) 